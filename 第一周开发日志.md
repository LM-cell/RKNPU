# 第一周开发日志（7.6-7.12）

## 一、本周工作内容

### 1. 阅读 RKNPU 技术报告，了解现有工作及后续优化方向

本周首先阅读了周雨同学关于 RKNPU 的技术报告，重点了解当前 StarryOS 平台上 RKNPU 驱动开发工作的整体目标、实现方案以及目前存在的问题。

RKNPU 开发的主要目标是让 StarryOS 能够真正使用 RK3588 平台的 NPU 计算资源，使上层推理程序或 benchmark 能够在 StarryOS 环境下直接提交 NPU 计算任务，而不再依赖 Linux 内核驱动和官方 RKNN 运行环境。

结合现有技术报告，对后续工作方向进行了初步分析：

#### （1）tiny_dispatch 小任务场景下的调度开销优化

现有工作表明，在小任务场景下，NPU 实际计算时间较短，固定调度开销会对整体性能产生较大影响。

由于一次 NPU 任务执行过程中存在任务提交、调度、yield、IRQ 回收以及同步锁竞争等额外开销，下一阶段计划对 blocking submit 的执行流程进行拆分，分析不同阶段的时间占比，定位主要性能瓶颈，并针对性优化调度流程，降低固定开销。

#### （2）三核 NPU 场景下尾延迟优化

当前三核 NPU 并行执行能够提升整体吞吐量，但技术报告中指出三核场景下存在明显的尾延迟波动问题。

相比单核场景，三核场景 jitter span 达到 9.84%，明显高于 1-core 场景的 0.03%。因此后续计划针对多核 NPU 执行过程中的延迟变化进行测试，分析任务调度、资源竞争等因素对尾延迟的影响，并进一步优化调度策略，提高多核执行稳定性。

#### （3）多线程并发提交场景验证

目前 benchmark 主要验证的是单次 blocking submit 下的多核扩展能力，对于多线程同时提交 NPU 任务时的竞争情况尚未充分验证。

后续计划增加多线程并发 submit 测试，分析多个任务同时提交时调度器的正确性、资源竞争情况以及系统稳定性。


---

### 2. 学习 NPU 基础知识，了解 AI 推理执行流程

学习了 NPU 的基本工作原理以及 AI 模型在专用推理硬件上的执行流程。

初步了解了一次 AI 推理任务从用户态提交，到驱动调度，再到 NPU 硬件执行的完整流程，包括：

- 用户程序提交推理任务；
- 驱动接收任务并进行资源管理；
- 调度器分配 NPU Core；
- NPU 执行计算任务；
- 完成后通过中断通知驱动更新任务状态。

通过该过程对 AI 推理软件栈和硬件执行流程建立了整体认识。


---

### 3. 阅读 RK3588 NPU 硬件资料，了解硬件架构

结合 RK3588 NPU 相关硬件手册，对 RK3588 NPU 的基本结构和主要硬件模块进行了初步学习。

重点了解：

- RK3588 NPU 的多 Core 架构；
- NPU 计算资源分配方式；
- AI 推理任务与硬件 Core 之间的关系；
- 软件调度层与硬件执行层之间的交互。


---

## 二、三个月实习总体规划

### 总体目标

完成 StarryOS 平台下 RKNPU 驱动现有工作的复现，在 RK3588 实机环境中重新进行性能测试，并围绕：

1. tiny_dispatch 小任务调度开销；
2. 三核 NPU 尾延迟波动；
3. 多线程并发任务提交；

三个方向开展性能分析、优化和实验验证。


### 第一阶段：基础学习、环境搭建与现有工作复现

主要完成：

- 熟悉 RKNPU 驱动架构；
- 完成 StarryOS 与 RKNPU 环境搭建；
- 理解 NPU 任务提交和调度流程；
- 在 RK3588 实机环境复现已有 benchmark；
- 补充多线程并发提交测试。


### 第二阶段：tiny_dispatch 小任务开销分析与优化

针对小任务场景下调度开销占比较高的问题：

- 拆分一次 blocking submit 的完整执行流程；
- 分析任务提交、调度、同步、中断处理等阶段耗时；
- 定位主要开销来源；
- 针对关键路径进行调度优化。


### 第三阶段：三核尾延迟分析、多线程验证与总结

针对三核 NPU 场景：

- 分析多 Core 并行情况下尾延迟变化；
- 定位 jitter 来源；
- 优化任务调度策略；
- 完善多线程并发测试；
- 整理三个月复现、测试和优化结果。


### 其他探索方向

尝试将当前 RKNPU 调度器独立封装为 Rust crate，提高代码复用能力，使不同硬件平台的 NPU 驱动能够复用统一调度模块。


---

## 三、下周计划

1. 进一步熟悉 RKNN Toolkit2 工作流程，完成模型转换和基础仿真验证，梳理 AI 模型从用户态到 NPU 硬件执行的完整流程。

2. 在 RK3588 开发板上部署 StarryOS（NPU 版本），运行 RKNPU 测试例程，验证基础运行环境。

3. 阅读 RKNPU 驱动代码框架以及 DRM 相关代码，进一步了解 NPU 驱动初始化、任务提交和硬件交互流程。
