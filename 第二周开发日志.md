# 第二周开发日志（7.13-7.18）

本周主要完成以下三方面工作：

------------------------------------------------------------------------

## 一、RK3588 AI 推理环境搭建与 RKNN 模型部署流程验证

围绕讯为 RK3588 开发板完成 AI 推理环境搭建与 RKNN模型部署流程验证，重点熟悉 Rockchip NPU 推理框架 RKNN的整体工作流程，打通从 PC 端模型转换到开发板端 NPU 推理的完整链路。

首先在 Ubuntu x86 平台完成 rknn-toolkit2 开发环境部署，搭建 PC端模型转换环境。完成 RKNN 模型生成后，在 PC 端进行了模型转换结果验证。

完成 PC 端模型转换后，将生成的 RKNN 模型部署到讯为 RK3588开发板，最后完成 RK3588 开发板上的 RKNN 模型推理测试。通过本次环境搭建和测试，完成了从模型转换、模型部署到 NPU推理验证的完整流程，对 RK3588 AI 模型部署流程进行了整体梳理。

一次AI模型完整部署和推理的流程如下：

``` text
训练模型
    ↓
ONNX模型
    ↓
rknn-toolkit2模型转换
    ↓
RKNN模型
    ↓
部署到RK3588
    ↓
rknn-toolkit-lite2加载
    ↓
RKNPU Runtime调用
    ↓
NPU推理
```

同时理解并区分了 PC 端和 RK3588 端在整个部署流程中的作用，以及rknn-toolkit2 与 rknn-toolkit-lite2 的区别：

### PC端

在模型转换阶段使用rknn-toolkit2，主要负责模型开发和部署前处理，将通用深度学习模型转换为适用于RK3588 NPU 的 RKNN 模型。

### RK3588端

在模型运行阶段使用 rknn-toolkit-lite2，主要负责模型加载和实际推理，在RK3588 平台上运行已经转换完成的 RKNN 模型，实现 NPU 硬件加速推理。

------------------------------------------------------------------------

## 二、在 RK3588 上部署 StarryOS（NPU版本）

我手上的板子是讯为 RK3588 开发板，根据StarryOS（NPU版本）的说明进行系统部署。

首先在 PC 端配置好开发环境，并下载相关镜像和安装工具后，在进行镜像编译和烧录过程中发现，当前提供的OS镜像并不适配手上的讯为 RK3588开发板，需要针对当前板子的硬件配置进行适配，包括：

-   boot 分区修改；
-   设备树（Device Tree）修改。

根据此问题先和周雨交流后，发现很难进行硬件配置适配，准备等申请的香橙派开发板到了后，再重新进行StarryOS（NPU版本）的适配和部署验证。

------------------------------------------------------------------------

## 三、阅读 RKNPU 代码，梳理仓库

阅读了 RKNPU 中 task 和service（任务怎么分配怎么调度的）部分的代码，主要对当前使用的调度算法`dispatch_idle_cores`进行了梳理。

当前调度器采用“Running 优先、队列内部按 priority 排序”的非抢占式调度策略。每轮调度先回收已经完成的NPU Core，再从 Running Submit 中按 priority 查找可调度 Lane  （`find_running_candidate_for_core`）；没有才从 Ready中按优先级（`pop_ready_candidate_for_core`）选择 Submit；随后把一条空闲的 Lane 的下一个 Task给绑定到允许使用的空闲 NPU Core，任务完成后通过中断更新 Lane状态，再重新进入下一轮调度。

```text
Running Submit
	↓ 
寻找可运行Lane
	↓ 
无可运行任务 
	↓ 
Ready Submit
	↓ 
按priority选择Submit
	↓ 
绑定空闲Lane和NPU Core
	↓ 
Task执行
	↓ 
中断更新Lane状态 
    ↓ 
下一轮调度  
```

当前调度策略优先保证已经启动的 Submit 连续执行，通过利用 RK3588 三个 NPUCore 提高吞吐量，保持任务连续性。但是这种调度方式可能会牺牲高优先级任务的响应时间，即使 Ready 队列中存在更高优先级的 Submit，也可能需要等待低优先级 Running Submit任务完成 。

------------------------------------------------------------------------

# 下周计划

1.  在适配过的 OrangePi 5 Plus 开发板上配置好StarryOS（NPU版本），跑通测试用例。
    
2.  测试当前 RKNPU已有代码，根据仓库的开发流程，分块软件模拟测试，再进行 OrangePi 5 Plus硬件平台上的实际测试。
