# 第三周开发周报（07.20—07.25）

## 一、本周工作概述

本周工作围绕主要 RK3588 开发板上的 Orange Pi 5 Plus（RK3588） U-Boot 适配、StarryOS （NPU） 部署验证、RKNPU 调度代码逻辑梳理和仿真测试以及 NPU 多核版本的部署测试这几个方面进行。

目前已补齐 U-Boot 的 `loady` 串口下载能力，完成 StarryOS RKNPU 版本部署跑通测例，`rknpu` crate 的模拟测试全部通过。

但 StarryOS 在 Orange Pi 5 Plus 上完整的NPU多核基准的最终性能数据仍需继续验证。

### 一、完成了 Orange Pi 5 Plus（RK3588） U-Boot 适配与启动验证

### 1. U-Boot 编译与烧录

##### 原来的问题：

Orange Pi 5 Plus板子原来的U-Boot 中没有：

```
loady和网卡驱动
```

而 ostool 的串口上传流程需要执行：

```
loady 0x10000000 或者 TFTP网络加载
```

该命令依赖 U-Boot 的 YMODEM 下载功能，因此原来的RK3588板子的 U-Boot 无法配合 ostool 完成 FIT 镜像传输。

针对上述问题，在 U-Boot 配置中启用 `CONFIG_CMD_LOADB`，重新编译并生成以下启动文件：

- `idbloader.img`
- `u-boot.itb`
- `u-boot-rockchip.bin`

将其写入 TF 卡启动U-Boot 成功提供 `loady` 命令，使 ostool 能够通过 YMODEM 将 FIT 镜像上传，

补齐 了`loady` 串口下载能力

**编译了U-boot网卡驱动，存在两个问题**：

1. U-Boot 启动后仍需要手动执行指令才能使用TFTP传输，每次重新上电又需要重新配置；
2. ostool 尚未进入自动 TFTP 分支，使用ostool仍会回退到 `loady`传输。

现在不能通过ostool 自动 TFTP传输， 只能通过 U-Boot 启动时输入一些指令，配置网卡设置静态ip进行TFTP通信 传输速但是很麻烦

后续还可以编译实现 U-Boot 的Fastboot ，配合typec可以自己输入指令但是传输FIT 镜像速度可以快很多。**（进行中但还存在有一些问题）**

<img src="C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260724213550795.png" alt="image-20260724213550795" style="zoom:50%;" />

## 二、StarryOS RKNPU 版本部署与验证　

本周完成了 StarryOS RKNPU 版本在 RK3588 开发板上的部署，并运行了现有测试程序和性能对比程序，确认系统能够进入 NPU 驱动测试流程。

<img src="C:\Users\Administrator\Desktop\RKNPU\部署StarryOS.png" alt="部署StarryOS" style="zoom:33%;" />

<img src="C:\Users\Administrator\Desktop\RKNPU\性能对比测试.png" style="zoom:33%;" />

## 三、RKNPU 驱动模拟测试用例

------

## 调度优先级测试修正

### 1.测试问题

失败测试为：

```
task::taskqueen::tests::running_submit_is_prioritized_before_new_ready_submit:647-668
```

原测试执行顺序为：

```
let running_id = queue.enqueue(running);
let ready_id = queue.enqueue(ready);

assert_eq!(
    queue.reserve_next_dispatch(0).unwrap().task_id,
    running_id
);
```

测试虽然将第一个对象命名为 `running`，但变量名不会改变对象状态。调用：

```
queue.enqueue(running)
```

只会把该提交放入 ready 队列，并不会让它进入 running 状态。

因此，第一次调度时队列中实际存在两个 ready submit：

| Submit    | 优先级值 | 实际状态 |
| --------- | -------- | -------- |
| `running` | 10       | ready    |
| `ready`   | -10      | ready    |

当前调度选择规则是：

```
优先级数值越小，调度优先级越高
```

所以调度器会选择优先级为 `-10` 的ready。

### 2 相关数据结构分析

#### `RknpuQueuedSubmit`

`RknpuQueuedSubmit` 表示一个进入调度器的 NPU 提交任务，其中包含：

- ioctl 提交参数；
- 任务数组；
- submit ID；
- 优先级；
- lane与NPU核心布局；
- 已派发、运行中和已完成状态；
- 最终完成目标数量。

测试用例中：

```
RknpuQueuedSubmit::new(
    fake_submit(2, 10, &[(0, 0, 1), (1, 1, 1)]),
    vec![fake_task(0x100), fake_task(0x200)],
);
```

该提交包含两个任务，并提供两条可派发 lane：

```
lane 0 → core 0
lane 1 → core 1
```

#### Ready与Running状态

提交刚经过：

```
queue.enqueue(submit)
```

后处于 ready 状态。

只有调用：

```
queue.reserve_next_dispatch(core_id)
```

成功为其保留并下发一条 lane 后，该提交才真正进入 running 状态。

因此，测试必须通过真实的调度操作完成状态转换，而不能通过变量名模拟运行状态。

### 3. 正确的测试流程

修正后的调度顺序为：

```
1. 创建包含两条lane的旧提交
2. 将旧提交加入ready队列
3. 在core 0上派发第一条lane
4. 旧提交进入running状态
5. 加入优先级更高的新提交
6. 在core 1上进行下一次调度
7. 验证调度器优先选择正在运行的旧提交
```

核心测试代码为：

```
#[test]
fn running_submit_is_prioritized_before_new_ready_submit() {
    let mut queue = TestBuckets::new();

    let running = RknpuQueuedSubmit::new(
        fake_submit(2, 10, &[(0, 0, 1), (1, 1, 1)]),
        vec![fake_task(0x100), fake_task(0x200)],
    );

    let ready = RknpuQueuedSubmit::new(
        fake_submit(1, -10, &[(0, 0, 1)]),
        vec![fake_task(0x300)],
    );

    // 旧提交先进入ready队列
    let running_id = queue.enqueue(running);

    // 向core 0派发第一条lane，使旧提交真正进入running状态
    assert_eq!(
        queue.reserve_next_dispatch(0).unwrap().task_id,
        running_id
    );

    // 再加入一个优先级更高的新提交
    let ready_id = queue.enqueue(ready);

    // 正在运行的提交应优先向core 1派发剩余lane
    assert_eq!(
        queue.reserve_next_dispatch(1).unwrap().task_id,
        running_id
    );

    assert_ne!(running_id, ready_id);//验证两个提交不相同
}
```

最终正确的调用顺序为：

```
enqueue running
→ dispatch core 0
→ enqueue ready
→ dispatch core 1
```

该流程同时验证了：

- ready到running的状态转换；
- running submit优先分发；
- 多核lane的分发；

------

## 4. 最终测试结果

完成上述修正后，`rknpu` crate 的全部主机单元测试通过：

![image-20260724214855717](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260724214855717.png)

本次测试覆盖的主要软件调度算法逻辑包括：

- ioctl命令分发和参数处理；
- 后台worker单例创建；
- Submit优先级调度；
- Ready与Running队列状态转换；
- 多核lane并行派发；
- 同一lane重复派发保护；
- 多核心完成状态回收；



修正后，主机端29项单元测试全部通过，说明当前 `rknpu` 的 ioctl、软件调度逻辑已经打通。

但当前结果仍属于主机端软件测试，尚不能证明真实硬件路径完全正确。后续还需要在 RK3588 + StarryOS 环境中继续验证：

### RKNPU 在开发板上进行多核验证测试

**问题：**程序能够识别 RKNPU 驱动，并正常进入 `core=1 warmup round 1/1`，说明测试程序已经能够打开 NPU 设备、完成参数解析和测试资源准备，并进入阻塞式 Submit 执行阶段。

<img src="C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260724213840473.png" alt="image-20260724213840473" style="zoom:50%;" />

本周未解决的问题：

**需要进一步定位阻塞 Submit 的原因。**  
如果测试长时间停留在 warmup，需要结合调度器日志检查 Task 是否已经下发、IRQ 是否产生、`core_binding` 是否释放以及 `harvest_completed_cores()` 是否正确更新 Lane 状态。

## 下周工作计划


1、学习使用GDB调试
2、分析现有的RKNPU代码，进行多核NPU性能测试



 
