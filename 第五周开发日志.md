# 第五周开发日志（8.3—8.8）

## 一、本周工作

本周主要围绕 **RKNPU 多核调度性能测试与并发 Submit 性能验证** 进行。

第一，按照前期技术报告中的 Benchmark 场景进行多轮重复实验，对比单核与三核执行性能，并重点分析三核路径下的延迟分布和稳定性。其中`mid_balanced unique` 和 `llama_decode_like shared` 两个场景的延迟波动较大，因此进一步统计 Mean、P50、P90、P95 等指标，判断波动来源。

第二，进行**多线程并发 Submit 测试**，通过多个线程同时提交独立 Submit，验证多个 Ready Submit场景下的并发派发、空闲核心选择、`core_mask` 限制、多线程竞争情况下的调度行为，并分析线程数增加对平均延迟、尾延迟和吞吐的影响。

------

## 二、实验一：单次 Submit 内多任务的单核/三核性能对比

整体来看，所有测试场景均能获得三核加速，但多数场景距离理想 3× 扩展仍存在明显差距。

| 场景              | 模式   | 单核 Mean  | 三核 Mean  | 加速比     | 并行效率   |
| ----------------- | ------ | ---------- | ---------- | ---------- | ---------- |
| tiny_dispatch     | Shared | 2.569 ms   | 1.439 ms   | 1.785×     | 59.50%     |
| tiny_dispatch     | Unique | 3.494 ms   | 1.759 ms   | 1.986×     | 66.20%     |
| mid_balanced      | Shared | 32.407 ms  | 17.790 ms  | 1.822×     | 60.72%     |
| mid_balanced      | Unique | 10.435 ms  | 5.872ms    | 1.777×     | 59.23%     |
| throughput_heavy  | Shared | 42.578 ms  | 20.886 ms  | 2.039×     | 67.95%     |
| throughput_heavy  | Unique | 6.781 ms   | 3.920 ms   | 1.730×     | 57.66%     |
| llama_decode_like | Shared | 345.437 ms | 133.607 ms | **2.585×** | **86.18%** |

其中 `llama_decode_like` 的计算占比较高，三核达到 **2.585× 加速、86.18% 并行效率**；其余多数场景并行效率约为 **57%～68%**，说明多核调度已经产生实际收益，但软件调度、同步以及共享资源开销仍限制了线性扩展。



### 延迟波动重点分析

重点关注`mid_balanced unique`和`llama_decode_like shared`两个三核场景

| 场景              | 模式   | Jitter：1核→3核 | 三核Min/Max       |
| ----------------- | ------ | --------------- | ----------------- |
| mid_balanced      | shared | 0.23%→28.02%    | 13.581/18.748ms   |
| llama_decode_like | shared | 0.13%→20.75%    | 111.859/139.581ms |

| 场景                     | 核数 | Mean    | P50     | P90     | P95     | P99     |
| ------------------------ | ---- | ------- | ------- | ------- | ------- | ------- |
| mid_balanced Unique      | 1核  | 10.435  | 10.436  | 10.439  | 10.441  | 10.464  |
| mid_balanced Unique      | 3核  | 5.872   | 6.043   | 6.073   | 6.074   | 6.077   |
| llama_decode_like Shared | 1核  | 345.437 | 345.426 | 345.527 | 345.546 | 345.591 |
| llama_decode_like Shared | 3核  | 133.607 | 135.675 | 139.525 | 139.551 | 139.564 |

`mid_balanced unique` 三核的大 Jitter 是少量低延迟样本影响的结果，P50-P99差别不大。正常轮次的延迟实际上是稳定的。

`llama_decode_like` 三核的 P50 到 P95 相差约 3.88 ms，存在一部分的更慢的延迟在139ms附近，但 P90、P95、P99 数值集中，仍较为稳定，没有发现少数异常慢任务。

------

## 三、实验二：多线程并发 Submit 性能测试

### 1. 三核NPU下不同提交线程数

使用mid_balanced场景

| 线程数 | Mean     | P50      | P95      | P99      | Throughput      |
| ------ | -------- | -------- | -------- | -------- | --------------- |
| 1      | 0.307 ms | 0.307 ms | 0.309 ms | 0.310 ms | 3254.78 task/s  |
| 2      | 0.747 ms | 0.691 ms | 0.860 ms | 0.924 ms | 2217.84 tasks/s |
| 3      | 1.320 ms | 1.319 ms | 1.552 ms | 1.626 ms | 1852.50 tasks/s |
| 6      | 5.144 ms | 4.455 ms | 5.186 ms | 5.644 ms | 983.15 tasks/s  |

线程数从 **1 增加到 3** 后：

- Mean：`0.307 → 1.320 ms` 
- P99：`0.310 → 1.626 ms`
- Throughput：`3254.78 → 1852.50 tasks/s`，下降约 **43.1%**

说明在当前 `mid_balanced` 负载下，增加提交线程数并没有带来更高的有效吞吐，反而使平均延迟和尾延迟明显上升。表明过量并发主要增加了驱动侧任务排队、同步竞争以及完成唤醒等软件开销。

### 2. 固定 3 个提交线程，改变 NPU 核心数

| NPU核心数 | Mean     | P50      | P95      | P99      | Throughput      |
| --------- | -------- | -------- | -------- | -------- | --------------- |
| 1核       | 1.603 ms | 1.468 ms | 1.995 ms | 2.134 ms | 1236.50 tasks/s |
| 2核       | 1.559 ms | 1.506 ms | 1.740 ms | 1.831 ms | 1520.48 tasks/s |
| 3核       | 1.335 ms | 1.327 ms | 1.561 ms | 1.634 ms | 1796.39 tasks/s |

1核扩展到3核后：

- Mean：下降 **16.72%**
- P50：下降 **9.60%**
- P95：下降 **21.75%**
- P99：下降 **23.43%**
- Throughput：提升 **45.28%**

说明固定3个提交线程时，增加NPU核心能够有效提升并发处理能力，改善尾延迟。

## 下周计划

一、把RKNPU复现帮助提交到主仓库中；
二、 分析NPU计算的过程，形成分析文档，给出可能的改进建议；
