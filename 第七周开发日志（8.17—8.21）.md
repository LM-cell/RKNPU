# 第七周开发日志（8.17—8.21）

## 本周工作

本周主要围绕 RKNPU Worker 等待、三核续派和混合 Task 调度开展优化与验证，完成以下工作[Commits](https://github.com/LM-cell/RKNPU/commit/211b1918509953db40082077a33eafd03b76b8a6)：

- `yield_now()` 的协作式轮询等待机制，换成IRQ/Event驱动的阻塞唤醒，由 NPU IRQ 唤醒 Worker；
- 实现 Submit 内动态 Task 派发，使空闲核心可以继续领取同一 Submit 的未执行 Task；
- 使用混合 Task 对比静态 Lane 与动态 Task 派发。

### Yield 轮询流程

原始实现使用基于 `yield_now()` 的协作式轮询[`worker_main()`](https://github.com/qc-gpu-driver/starryos-pulsar/blob/58c171092eae2b682810287411abac6c1fca2b12/drivers/rknpu/src/service/scheduler.rs#L769)

```
dispatch Task
→ NPU 运行
→ Worker 调用 yield_now()
→ Worker 重新获得 CPU
→ harvest 检查 irq_status
→ 未完成则继续 yield
→ 完成后回收并续派
```

该方案不会持续占据 CPU 执行，但 Worker 始终保持 Ready，并在每次重新获得 CPU 后主动检查 completion，因此本质上仍属于让出式轮询。

### 当前 Event/Waker 流程

[`worker_main()`](/C:/Users/Administrator/Desktop/starryos-pulsar/drivers/rknpu/src/service/scheduler.rs:1273)。

```
注册 Listener
→ harvest
→ dispatch
→ 确认仍有 in-flight Task
→ listener.wait()
→ Worker 进入 Blocked
→ NPU 产生 IRQ
→ IRQ 发布 irq_status
→ kick.notify_one()
→ Waker 唤醒并请求重新调度
→ harvest 并续派
```

IRQ路径仍只负责读取并清除硬件中断、发布 completion 状态和通知 Worker，没有在硬中断上下文中执行 Task 回收、队列操作或 NPU续派

## 实验对比

| 场景              | Yield 1核 Mean | Yield 3核 Mean | Event 1核 Mean | Event 3核 Mean | Event 3核相对 Yield 3核 | Event 三核加速比 |
| ----------------- | -------------: | -------------: | -------------: | -------------: | ----------------------: | ---------------: |
| tiny_dispatch     |       1.638 ms |       1.157 ms |       1.951 ms |       1.116 ms |                 快 3.5% |            1.75× |
| mid_balanced      |      32.666 ms |      18.818 ms |      33.917 ms |      13.687 ms |                快 27.3% |            2.48× |
| throughput_heavy  |     134.924 ms |      73.685 ms |     136.578 ms |      74.098 ms |                 慢 0.6% |            1.84× |
| llama_decode_like |     344.723 ms |     132.576 ms |     354.973 ms |     119.035 ms |                快 10.2% |            2.98× |

###  数据结论

- 相比 Yield 三核，Event 三核在 `mid_balanced` 和 `llama_decode_like` 上分别缩短 27.3% 和 10.2%。
- `throughput_heavy` 的 Event 三核 Mean 慢 0.6%，说明 Event/Waker 并非对所有任务粒度都更快。
- Event/Waker 的收益取决于减少 completion 轮询的收益，能否覆盖阻塞、唤醒和重新调度的开销。

## 混合 Task 的动态调度

### 实现内容

混合负载由 48 个相互独立的矩阵 Task 组成：12 个 tiny、12 个 mid、12 个 heavy 和 12 个 llama。

静态模式继续按三个 Lane 的游标推进，同一 Lane 同时最多有一个 in-flight Task。

动态模式使用 `dynamic_next_task` 维护单个 Submit 内的顺序游标。哪个允许使用的物理核心先空闲，哪个核心就领取该 Submit 的下一个 Task[`next_dispatchable_task()`](/C:/Users/Administrator/Desktop/starryos-pulsar/drivers/rknpu/src/task/taskqueen.rs:370)。

### Static 与 Dynamic 结果

以下数据对应单线程、`submit_limit=1`、`clustered` 布局，负载和 Task 数保持一致，只改变静态/动态派发模式：

| 指标       |         Static |        Dynamic |   变化 |
| ---------- | -------------: | -------------: | -----: |
| Mean       |     122.898 ms |      94.740 ms | -22.9% |
| Throughput | 390.55 tasks/s | 506.63 tasks/s | +29.7% |

四阶段均值如下：

| 阶段             |     Static |   Dynamic |
| ---------------- | ---------: | --------: |
| submit_prepare   |   0.009 ms |  0.009 ms |
| queue_wait       |   3.755 ms |  3.769 ms |
| dispatch_execute | 114.670 ms | 86.520 ms |
| complete_return  |   4.456 ms |  4.434 ms |

差异主要集中在 `dispatch_execute`。

### 四阶段延迟来源

`SubmitLatencyTrace` 只随调度对象保存 `t0`～`t3`。`t4` 在完成 Task 数组和 Submit 头的 copy-out 后采样，最终五个时间点统一写入 [`RknpuSubmitTraceRecord`](/C:/Users/Administrator/Desktop/starryos-pulsar/drivers/rknpu/src/ioctrl.rs:140)：

```rust
pub struct RknpuSubmitTraceRecord {
    pub queue_task: u64,
    pub t0_ns: u64, // ioctl 进入驱动
    pub t1_ns: u64, // Submit 进入调度队列
    pub t2_ns: u64, // 第一个 Task 开始底层下发
    pub t3_ns: u64, // 最后一个 Task 的完成状态被 Worker 收割
    pub t4_ns: u64, // Task 数组和 Submit 头完成 copy-out
}
```

四阶段分别为：`submit_prepare=t1-t0`、`queue_wait=t2-t1`、`dispatch_execute=t3-t2`、`complete_return=t4-t3`

## 5. 下周计划

当前实现是由 Event/Waker 阻塞唤醒的内核 Worker，还不是完整的异步协程调度器，继续改进。