# **第六周周报（8.10-8.15）**

## **1. RK3588 官方 RKNN Runtime 多核执行**

**1.RK3588 官方 RKNN Runtime 支持通过 `rknn_set_core_mask()` 指定模型可使用的 NPU Core，包括单核 `CORE_0/1/2`、双核 `CORE_0_1` 和三核 `CORE_0_1_2` 等组合。`rknn_run()` 用于发起一次模型推理，`rknn_wait()` 用于等待异步推理完成；即 Core mask 决定允许使用哪些 NPU Core，run/wait 则负责推理执行与完成同步。**

> 相关官方源码：[`rknn_core_mask`（L243-L256）](https://github.com/airockchip/rknn-toolkit2/blob/59a913d172e7f5ff03c9076e2ec7b1b1288ffd08/rknpu2/runtime/Linux/librknn_api/include/rknn_api.h#L243-L256) ｜ [`rknn_set_core_mask()` / `rknn_run()` / `rknn_wait()`（L558-L602）](https://github.com/airockchip/rknn-toolkit2/blob/59a913d172e7f5ff03c9076e2ec7b1b1288ffd08/rknpu2/runtime/Linux/librknn_api/include/rknn_api.h#L558-L602) ｜ [`rknn_run_extend`（L457-L465）](https://github.com/airockchip/rknn-toolkit2/blob/59a913d172e7f5ff03c9076e2ec7b1b1288ffd08/rknpu2/runtime/Linux/librknn_api/include/rknn_api.h#L457-L465)

假如一个视频模型做推理，用户态最容易控制的最小调度单位通常是：

**一次 inference request，一帧视频 = 一次** **`rknn_run()`。**

Worker 取一帧当Task进行调度， 从应用层来看这个就是最小调度Task，**做多帧并行计算时就是独立的多个Task在多个npu上运行,这样可以保证任务多的情况下多核一直吃满。**

Rockchip官方也支持单帧推理使用多核 “single model on multiple cores”：[`CHANGELOG.md#L93-L103`](https://github.com/airockchip/rknn-toolkit2/blob/master/CHANGELOG.md#L93-L103)， **允许一帧使用多个npu,但是不能保证三个 Core 都能一直吃满，但是内部切分由RKNN Compiler/Runtime 管理，这部分官方公开 API 没有提供底层 Task 切分细节 。**

> 官方对应版本记录：[`CHANGELOG.md` v1.4.0：RK3588 single model on multiple cores](https://github.com/airockchip/rknn-toolkit2/blob/59a913d172e7f5ff03c9076e2ec7b1b1288ffd08/CHANGELOG.md#L113-L123)

------

## **2. NPU 最小 Task怎么划分的？**

**标准 RKNN 模型**的底层硬件工作边界由 RKNN Compiler/Runtime 内部形成，公开 API 并不暴露一个 inference 最终对应多少个底层 `RknpuTask`，也不允许用户直接指定某个硬件 Task 的边界。

而**当前RKNPU**将 Task/regcmd 生成显式放在用户态测试程序中，使 Task 边界、Lane 和 Core 调度可以直接研究。[`build_task_descriptors()`：用户态生成 regcmd 并构造 `RknpuTask[]`（L491-L536）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/demo/npu_benchmark/tests/core_scaling_benchmark.c#L491-L536) ｜ [`gen_matmul_fp16()`（L202-L211）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/demo/npu_benchmark/src/npu_matmul.c#L202-L211)

------

## **3. 当前RKNPU的Task划分**

当前RKNPU的任务划分：最小硬件调度单位是 `RknpuTask`，用户态测试程序生成 regcmd 和 `RknpuTask[]`，所以 Task 边界目前主要由用户态程序定义；

> 前提：一个最小 `RknpuTask` 必须已经是一个可以独立下发到某个 NPU Core、从开始一直执行到完成 IRQ 的完整硬件执行单元。**读可以共享，写分区独立，Task间有依赖关系要处理好**。

每个 Task 必须指向一份完整可执行的 regcmd，并具备明确的输入、输出、资源和完成状态。多个Task 被集合进一个 Submit，再由 Scheduler 在运行时把各 Lane 当前可执行的 Task 动态派发到允许且空闲的物理 NPU Core；Task 完成后通过 IRQ 回收并推进 Lane，直到 Submit 内全部 Task 完成。

```
       用户态测试程序
         │
         ├─ 决定计算问题 
         ├─ 生成 regcmd
         └─ 构造 RknpuTask[]
               	 │                 
             	Submit
				 │
   Task0 Task1 Task2 Task3 Task4 Task5
                 │
          subcore_Task[]
                 │
       ┌─────────┼─────────┐
       ▼         ▼         ▼
     Lane0     Lane1     Lane2
   T0 → T1   T2 → T3   T4 → T5
       │         │         │
       └─────────┼─────────┘
                 ▼
             Scheduler
                 │
       选择满足core_mask的空闲Core
                 │
       ┌─────────┼─────────┐
       ▼         ▼         ▼
     Core 0    Core 1    Core 2
```

**当前可以并行的是来自不同可执行 Lane 的多个** **`RknpuTask`，但不支持同一个** **`RknpuTask`** **同时占用多个 NPU Core，并且同一个 Lane在同一时刻最多只能有一个 in-flight Task。**

相关源码：[`next_dispatchable_lane()` / `mark_lane_running()` / `complete_lane()`（L299-L355）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/task/taskqueen.rs#L299-L355) ｜ [`prepare_dispatch()`：Lane→physical Core `core_binding`（L199-L267）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/scheduler.rs#L199-L267) ｜ [`submit_ioctrl_step()`：one queued task → one physical core（L134-L214）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/ioctrl.rs#L134-L214) ｜ [`start_execute_one()`：`task_number = 1`（L52-L95）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/registers/mod.rs#L52-L95)

------

## **4. 当前 StarryOS/RKNPU 调度运行模型**

当前 StarryOS/RKNPU 已形成“用户态同步提交、驱动内异步执行”的 NPU 计算模型：用户态在提交前生成 regcmd 并确定 `RknpuTask` 边界，多个 Task 被组织为 Submit 和 logical Lane；内核 Scheduler 以 `RknpuTask` 为最小硬件派发单位，将各 Lane 当前可执行 Task 动态绑定到满足 `core_mask` 的空闲物理 Core。Task 下发后 NPU 独立执行，完成时通过 per-core IRQ 发布状态，由后台 Worker 在 Task completion boundary 回收 Task、释放 Core、推进 Lane 并继续派发后续工作；直到整个 Submit 进入terminal后，才通过 per-submit waiter 唤醒 blocking ioctl 调用线程并统一返回结果。

调度运行对应函数：[`handle_submit_ioctl()`：copy-in → enqueue → wait → terminal → copy-back（L100-L176）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/ioctl.rs#L100-L176) ｜ [`enqueue_submit()` / `wait_for_submit()` / `take_terminal_submit()`（L329-L420）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/scheduler.rs#L329-L420) ｜ [`StarrySubmitWaiter` / `StarryWorkerSignal`（L70-L132）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu-starry-adapter/src/card1.rs#L70-L132)

------

# **一次 NPU 计算任务的完整执行过程**

当前 StarryOS中RKNPU 的一次 NPU 计算可以概括为：

```
用户态定义计算问题
       ↓
生成 regcmd + 构造 RknpuTask[]
       ↓
组织为 Submit，并通过 subcore_Task[] 定义 logical lane
       ↓
blocking ioctl 进入 StarryOS
       ↓
copy-in Submit / Task，enqueue 到 Ready
       ↓
用户 ioctl 线程进入 waiter 睡眠
       ↓
后台 Worker 选择 Running/Ready Submit
       ↓
选择一个可执行 Lane 的当前 Task
       ↓
动态选择满足 core_mask 的 idle physical Core
       ↓
建立 core_binding
       ↓
submit_ioctrl_step()
       ↓
start_execute_one()：Task_number = 1
       ↓
PC 从 Task.regcmd_addr 执行完整 regcmd
       ↓
NPU CNA / CORE / DPU 等模块完成计算
       ↓
对应 Core 产生 IRQ
       ↓
驱动发布 per-core completion 状态
       ↓
Worker harvest completion
       ↓
解除 core_binding + 回写 int_status + lane cursor++
       ↓
继续派发后续 Task
       ↓
Submit 内所有 Task 完成
       ↓
Submit Terminal + waiter.complete()
       ↓
blocking ioctl 被唤醒并返回用户态
```

> 一次计算任务流程对应的源码：[`build_task_descriptors()`（L491-L536）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/demo/npu_benchmark/tests/core_scaling_benchmark.c#L491-L536) → [`handle_submit_ioctl()`（L100-L176）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/ioctl.rs#L100-L176) → [`dispatch_idle_cores()`（L680-L765）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/scheduler.rs#L680-L765) → [`submit_ioctrl_step()`（L134-L214）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/ioctrl.rs#L134-L214) → [`start_execute_one()`（L52-L95）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/registers/mod.rs#L52-L95) → [`handle_interrupt()`（L463-L501）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/registers/mod.rs#L463-L501) → [`harvest_completed_cores()`（L583-L676）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/scheduler.rs#L583-L676)

应明确区分三个边界：

1. **任务划分边界**：主要发生在用户态 Task/regcmd 生成阶段； [`core_scaling_benchmark.c#L491-L536`](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/demo/npu_benchmark/tests/core_scaling_benchmark.c#L491-L536) ****
2. **任务调度边界**：当前 Scheduler 的主要重新调度点位于一个 Task 的完成，完成Task并Worker 回收后，才释放对应 Core 并选择后续 Task。[`scheduler.rs#L583-L676`](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/scheduler.rs#L583-L676) 
3. **Submit 完成边界**：Submit 内进入 terminal 状态后，才唤醒 blocking ioctl 调用线程并统一回写结果。 [`scheduler.rs#L329-L420`](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/scheduler.rs#L329-L420)

------

# **现在主要的问题**

当前 StarryOS/RKNPU 的最小硬件派发单位是 `RknpuTask`，以 MatMul benchmark 为例，用户态测试程序先生成完整 `regcmd`，再构造 `RknpuTask[]`，因此 Task 边界在进入 Scheduler 之前已经确定‘了。

## **最小任务划分边界**

**最小任务划分边界:**怎么把一个大Task怎么拆成多个独立的小Task？ 这个“独立” 不好控制 ，需要保证每个子 Task 都能独立形成完整硬件执行单元，同时任务划分太小不好，有可能额外开销，现在RKNPU驱动将一个完整矩阵计算划分为一个Task就很好，而且用户态去设置更灵活 。

------

# **可以改进的地方**

## **1. Completion 等待尚未完全事件驱动**

当前调度运行模型是：

> **用户态同步阻塞、驱动内异步执行。**

用户线程通过 blocking ioctl 提交 Submit 后进入 waiter 睡眠，而后台 Worker 独立完成 Ready/Running 调度、Task 派发以及完成回收。

但是在存在 in-flight Task 时，Worker 当前采用 `yield → 检查 completion` 的方式等待硬件完成，IRQ 尚未直接作为事件唤醒 Scheduler Worker。

> 相关源码：[`worker_main()`：存在in-flight 时 `yield_now()` 再 harvest（L795-L850）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/service/scheduler.rs#L795-L850) 

现在有硬件任务在执行时:

```
harvest_completed_cores();
    ↓
yield_now();
    ↓
harvest_completed_cores();
```

也就是：

```
让出CPU
→ 再次被调度
→ 检查IRQ状态
→ 没完成继续让出CPU
```

**针对这个可以将当前 Worker 的** **`yield → 检查 completion`** **改为 NPU IRQ 发布完成状态后直接唤醒 Scheduler Worker，使 Core 完成后能够及时触发 Worker 回收和后续派发；**

> 现在的Worker 唤醒原语：[`StarryWorkerSignal::listen()` / `notify_one()`（L108-L132）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu-starry-adapter/src/card1.rs#L108-L132)

------

## **2. Task 调度仍受到固定 Lane 分配的约束**

当前 Ready/Running 调度对象仍然是 **Submit**，而不是所有 `RknpuTask` 组成的全局 Ready 队列。

报告对应：[RKNPU 技术报告](https://qc-gpu-driver.github.io/starryos-pulsar/develop_log/RKNPU技术报告.html) 

由于同一 Lane 同时最多只能有一个 in-flight Task，如果其他 Lane 已经提前执行完，而某一 个Lane 中仍有多个未执行 Task，那么即使另外两个 physical Core 已经空闲，这个 Lane 后续的多个 Task 也不能被多个空闲Core 领取。

> **相关源码：**
>  [`lane_isrun` / `subcore_cursors`（L125-L205）](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/task/taskqueen.rs#L125-L205) ｜ [`next_dispatchable_lane()`：`lane_isrun` 为 true 时跳过该 Lane](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/drivers/rknpu/src/task/taskqueen.rs#L299-L330) ｜ [`distribute_tasks_to_cores()`：用户态预先划分连续 `subcore_task[]`](https://github.com/LM-cell/RKNPU/blob/d3dc8098c3af8e4a776d672b08e674c5b44e6a26/demo/npu_benchmark/tests/core_scaling_benchmark.c#L547-L567\

**针对这个可以动态调整lane负载，当某些 Lane 已执行完、出现空闲 Core，而其他 Lane 仍有大量未执行 Task 时，允许空闲 Core 从长 Lane 的剩余 Task 中动态拿走待执行的Task**



## 相关数据结构

| **数据结构**        | **所在文件**                             | **作用**                                                     | **关键字段**                                                 |
| ------------------- | ---------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `RknpuTask`         | `drivers/rknpu/src/job.rs`               | 当前最小硬件派发任务描述符，描述一次完整 regcmd 执行         | `int_mask`、`int_status`、`regcfg_amount`、`regcmd_addr`     |
| `RknpuSubcoreTask`  | `drivers/rknpu/src/ioctrl.rs`            | 描述一个 logical Lane 所包含的连续 Task range                | `task_start`、`task_number`                                  |
| `RknpuSubmit`       | `drivers/rknpu/src/ioctrl.rs`            | 一次用户态提交的 ABI 数据结构，组织多个 `RknpuTask`、Lane 和可用 Core | `task_number`、`priority`、`core_mask`、`subcore_task[]`     |
| `SubmitMeta`        | `drivers/rknpu/src/task/taskqueen.rs`    | Scheduler 使用的 Submit 不可变元数据                         | `priority`、`core_mask`、`lane_ranges`、`task_total`         |
| `RknpuQueueTask`    | `drivers/rknpu/src/task/taskqueen.rs`    | 表示一个 Submit 在 Scheduler 中的运行时状态                  | `tasks`、`subcore_cursors`、`lane_isrun`、`last_error`       |
| `NpuSchedulerState` | `drivers/rknpu/src/service/scheduler.rs` | Scheduler 全局状态，维护 Submit 队列和物理 Core 绑定         | `tasks`、`ready`、`running`、`complete`、`core_binding`、`waiters` |
| `CoreRunBinding`    | `drivers/rknpu/src/service/scheduler.rs` | 记录某个 physical Core 当前运行的是哪个 Submit / Lane / Task | `task_id`、`lane_slot`、`task_index`                         |
